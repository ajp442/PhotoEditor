#ifndef IMAGE_H
#define IMAGE_H

#include <QImage>

//Simply trying to test something.... ~Josh

/*
class Image : public QImage
{
public:
    Image(QImage image);

    class Proxy
    {
    public:
        Proxy(QImage proxyImage, int row);
        QRgb operator[](int column);
    private:
        QImage _proxyImage;
        int _row;
    };

    Proxy operator[] (int row);
private:
    QImage _image;
};
*/
#endif // IMAGE_H
