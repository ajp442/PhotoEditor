#include "image.h"

//Simply trying to test something ~Josh

/*
Image::Image(QImage image) : QImage()
{
    _image = image;
}

Image::operator [](int row)
{
    return new Proxy(image, row);
}

Image::Proxy::operator [](int column)
{
    return _proxyImage.pixel(_row, column);
}

Image::Proxy::Proxy(QImage proxyImage, int row)
{
    _row = row;
    _proxyImage = proxyImage;
}
*/

